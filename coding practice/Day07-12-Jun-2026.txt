# Day 07 — SPEED TYPING

Rewrite ALL FOUR from scratch:

1. Ansible: Install nginx
2. Terraform: EC2 (choose any Pathnex instance)
3. Kubernetes: Deployment 2 replicas
4. Shell: Print date + uptime


1. Ansible — Install Nginx
---
- name: Install nginx
  hosts: all
  become: yes

  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present



2. Terraform — EC2 Instance
provider

 "aws" {
  region = "us-east-1"
}

resource "aws_instance" "PathnexEC2" {
  ami           = "ami-0abcd1234abcd1234"
  instance_type = "t2.micro"

  tags = {
    Name = "Pathnex-EC2"
  }
}


3. Kubernetes — Deployment (2 Replicas)

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pathnex-deployment

spec:
  replicas: 2

  selector:
    matchLabels:
      app: pathnex-app

  template:
    metadata:
      labels:
        app: pathnex-app

    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80


4. Shell Script — Print Date & Uptime

#!/bin/bash

echo "Current Date:"
date

echo "System Uptime:"
uptime


# Parameterized Builds
A Parameterized Build in Jenkins allows users to pass input values (parameters) at runtime, making the same pipeline reusable for different environments such as dev, QA, and production.

## 🔹 Jenkins Pipeline — Parameterized
You will learn how to **create parameterized builds in Jenkins**.

pipeline {
    agent any
    parameters {
        string(name: 'ENVIRONMENT', defaultValue: 'dev', description: 'Deployment Environment')
    }
    environment {
        INSTITUTE_NAME = "Pathnex"
    }
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/Pathnex/sample-java-app.git'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "Deploying to $ENVIRONMENT by $INSTITUTE_NAME"'
            }
        }
    }
}

## Summary: 
parameters → User input at build time.
environment → Fixed reusable variables.
Makes Jenkins pipelines flexible and reusable.


## 🔹 GitLab CI — Parameterized via Variables
You will learn how to **use variables to parameterize GitLab jobs**.

stages:
  - deploy

variables:
  INSTITUTE_NAME: "Pathnex"
  ENVIRONMENT: "dev"

deploy:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying to $ENVIRONMENT by $INSTITUTE_NAME"

## Summary: 
variables act like parameters/configuration values.
Can be changed without modifying job logic.
Makes GitLab pipelines reusable across environments.